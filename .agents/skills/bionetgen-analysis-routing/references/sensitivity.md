# Sensitivity

Use Sobol indices for global variance attribution under stated parameter bounds. Use FIM for local derivative-based distinguishability. Use a generic parameter scan for a finite grid whose values, not a sensitivity index, are the requested output.
