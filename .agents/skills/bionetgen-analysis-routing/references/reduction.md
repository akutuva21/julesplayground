# Reduction

Use `reduce_model` for fitted/pruned model reduction and `qssa_reduction` for fast-species quasi-steady-state candidates. Report approximation assumptions and validate the reduced model against the intended outputs.
