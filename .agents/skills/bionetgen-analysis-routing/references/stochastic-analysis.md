# Stochastic analysis

Use first-passage time for threshold crossing distributions, LNA for local Gaussian fluctuation approximations, and ordinary SSA simulation when trajectories themselves are the object. State seed, ensemble size, and limits.
