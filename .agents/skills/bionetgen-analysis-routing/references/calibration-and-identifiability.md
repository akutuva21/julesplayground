# Calibration and identifiability

`fit_parameters` searches for a point estimate. `identifiability_analysis` profiles data-constrained parameters and depends on experimental data. `compute_fim` summarizes local sensitivity/collinearity around a specified point. Do not convert a local rank into a prior-wide claim without robustness evidence.
