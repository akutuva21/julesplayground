# MCP Apps contract

Keep the five existing resource URIs stable. Assert resource metadata, MIME type, bundle self-containment, tool metadata, and ordinary structured/text results independently. An App may visualize a result but must not be required for correctness.
