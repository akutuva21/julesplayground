# Structured output

Every stable tool should return machine-readable `structuredContent` conforming to its declared output schema and a concise useful text summary. Do not duplicate huge trajectory/network payloads by default; use token-efficient modes or resources where available.
