# RuleHub resources

Use `rulehub://model/{id}` with manifest-validated IDs. Never turn an arbitrary URI suffix into a filesystem path or fetch a user-supplied URL. Search returns metadata/resource links; resource read returns exact BNGL plus provenance metadata.
