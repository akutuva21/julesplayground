# Stable tool admission

A new stable tool needs: distinct intent; no clean existing equivalent; engine-owned scientific implementation; strict bounded input; output schema; structured content; contrastive description; tested error semantics; integration fixture; appropriate scientific coverage; usefulness without an App; and controlled output size.
