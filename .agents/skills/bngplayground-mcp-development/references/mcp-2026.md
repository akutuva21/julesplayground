# MCP 2026-07-28

SDK v2 does not change the wire era by itself. Use `createMcpHandler(factory)` for web-standard HTTP and `serveStdio(factory)` for connection-pinned stdio negotiation. Do not hand-roll protocol negotiation or retain a private v1 compatibility bridge.
