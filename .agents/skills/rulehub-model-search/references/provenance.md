# Provenance checklist

Record:

- repository: `RuleWorld/RuleHub`;
- configured ref, normally `master`;
- exact manifest path;
- model ID;
- citation when supplied;
- retrieval timestamp;
- commit revision when resolvable, otherwise `null`.

Do not silently substitute a local stale checkout for authoritative MCP retrieval.
