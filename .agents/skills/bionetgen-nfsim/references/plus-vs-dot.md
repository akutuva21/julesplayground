# `+` versus `.`

`+` separates molecules/patterns as reaction participants. `.` joins molecules into one connected pattern. A disconnected pattern can express a different rule and different molecularity than a connected complex.
