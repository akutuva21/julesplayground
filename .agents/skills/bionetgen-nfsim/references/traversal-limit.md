# Traversal limits

Traversal/UTL failures usually indicate that NFsim cannot inspect a sufficiently large complex within configured limits. Report the limit and model scale; do not silently raise it and call the original run valid.
