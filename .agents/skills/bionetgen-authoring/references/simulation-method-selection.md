# Simulation method selection

Use network-based ODE/SSA when expansion is tractable and inspectable. Use NFsim when combinatorial expansion is the limiting factor and the model is compatible. A solver choice is not a validation result: validate first and compare with BNG2/NFsim parity when numbers matter.
