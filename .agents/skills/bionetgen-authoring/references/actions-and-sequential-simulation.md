# Actions and sequential simulation

Actions execute in order. A parameter/state change in one phase can persist into the next unless explicitly reset. For multi-phase workflows, record phase boundaries, parameter values, initial conditions, and the observable used to assess the handoff.
