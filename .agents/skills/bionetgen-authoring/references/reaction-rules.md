# Reaction rules

Check reactant/product graph changes, bond labels, state changes, rule reversibility, and molecularity. A plausible-looking rule can change kinetics if it disconnects a pattern, changes the number of reactant molecules, or omits a site condition. Prefer explicit named rules when editing a model.
