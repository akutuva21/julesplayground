# Model structure

Use the normal BNGL sections deliberately: parameters, molecule types, seed species, observables, reaction rules, and actions. Names are identifiers, not prose. Keep molecule type declarations consistent with every pattern. Validate section nesting and action ordering before interpreting a simulation.
