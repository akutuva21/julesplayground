# Observables

Choose `Molecules` versus `Species` intentionally. Match the observable pattern to the state/bond condition being measured and use stable names for downstream analysis. Validate that every observable parses and that its interpretation matches the intended biological quantity.
