# Molecule types

Declare every molecule and component used by seed species, observables, and rules. Preserve site order only when a downstream convention depends on it; semantic identity comes from names, states, and bonds. Do not use undeclared states as informal annotations.
