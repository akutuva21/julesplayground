2026-06-29: Replaced `getExpressionDependencies` ANTLR parsing in `isFunctionalRateExpr` (used in `PatternMatcher` and `NetworkExpansion` for fast simulation validation) with a custom zero-allocation character scanning loop. Reduced overhead from 19ms down to 5ms for 10k evaluations, significantly accelerating hot-path execution during complex rule evaluations and removing a major bottleneck.
