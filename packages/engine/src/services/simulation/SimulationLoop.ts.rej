--- packages/engine/src/services/simulation/SimulationLoop.ts
+++ packages/engine/src/services/simulation/SimulationLoop.ts
@@ -1436,6 +1436,10 @@
           console.log(`[Worker] Using KLU sparse method for analytical Jacobian (nnz=${csrMatrix.nnz})`);
         }

+        const compiledSSAPropensities = functionalRateCount === 0
+          ? jitCompiler.compileSSAPropensities(concreteReactions, reactionReactingVolumes)
+          : null;
+
         // Pre-allocate RNG buffer for bulk random number generation.
         // Calling Math.random() in a loop is expensive; generate in batches.
         const rngBuffer = new Float64Array(1024);
