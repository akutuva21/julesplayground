## 2025-05-18 - Figure Builder Toggle Buttons
**Learning:** Custom toggle buttons (like layout and width selectors) need `aria-pressed` for screen readers to understand their state, and explicit `focus-visible` styles for keyboard navigation.
**Action:** Always add `aria-pressed` and `focus-visible` utility classes to custom toggle UI elements.
